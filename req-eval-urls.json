{
  "action": "evaluate",
  "args": {
    "code": "(() => { return Array.from(document.querySelectorAll('a.search-title, a.title')).map(el => ({title: el.innerText, url: el.href})); })()"
  },
  "session": "business-ideas"
}
